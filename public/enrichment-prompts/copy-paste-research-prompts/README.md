# Copy/Paste Research Prompts

Generated from `docs/copy-paste-analysis-prompts/*.yaml` in the separate Alpha Edge Intelligence repository.

The UI must use only each YAML file's `copy_paste_prompt` field as the copyable prompt body. YAML metadata is retained only for filtering, search, and display.

Current generated prompt count: 47.
